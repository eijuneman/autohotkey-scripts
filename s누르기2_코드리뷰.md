# s누르기2.ahk 코드 리뷰 보고서

## 1. 메모리 누수 문제점

### 1.1 Excel COM 객체 미해제 (심각도: 높음)

**문제 위치:** 3448-3449줄, 3800-3801줄, 4758-4789줄

```autohotkey
wb := XL.ActiveWorkbook
ws := XL.ActiveSheet
; ... 작업 후 wb, ws 해제 없음
XL.Quit()
XL := ""
```

**문제점:**
- `wb`, `ws` 객체가 생성되지만 해제되지 않음
- COM 객체 참조가 남아있어 Excel 프로세스가 완전히 종료되지 않을 수 있음

**해결 방법:**
```autohotkey
; 작업 완료 후
wb := ""
ws := ""
XL.ActiveWorkbook.Close(0)
XL.Quit()
XL := ""
```

---

### 1.2 MySQL 연결 미해제 (심각도: 중간)

**문제 위치:** 113-118줄

```autohotkey
sql_start()
global myDB := dbConnect(host, user, pw, database)
; ... 스크립트 종료 시 dbDisConnect 호출 없음
```

**해결 방법:** OnExit 핸들러 추가
```autohotkey
OnExit, CleanUp
return

CleanUp:
dbDisConnect(myDB)
exit_sql()
ExitApp
```

---

### 1.3 image_backup 배열 누적 (심각도: 낮음)

**문제 위치:** 3866줄

```autohotkey
image_backup := {}
; Loop에서 계속 추가
; 함수 종료 후에도 전역으로 남아있음
```

**해결 방법:** 사용 후 명시적 해제
```autohotkey
; 작업 완료 후
image_backup := ""
```

---

### 1.4 result 변수 누적 (심각도: 낮음)

**문제점:** dbQuery 결과가 계속 `result` 변수에 재할당되지만, 대용량 결과의 경우 이전 결과가 즉시 해제되지 않을 수 있음

**해결 방법:** 중요한 작업 후 명시적 해제
```autohotkey
result := dbQuery(myDB, myQuery)
; 작업 처리 후
result := ""
```

---

## 2. 무한 루프 위험

### 2.1 ImageSearch 무한 루프 (심각도: 높음)

**문제 위치:** 434-450줄, 529-545줄 등 여러 곳

```autohotkey
Loop
{
    ImageSearch, n42x, n42y, ...
    if (errorlevel = 1)
    {
        sleep 100  ; 이미지 못찾으면 계속 대기
    }
    ; ... 탈출 조건이 errorlevel = 0 또는 2 뿐
}
```

**문제점:** 이미지를 영원히 찾지 못하면 무한 루프

**해결 방법:** 최대 시도 횟수 또는 타임아웃 추가
```autohotkey
maxAttempts := 100  ; 최대 100번 시도 (10초)
Loop, %maxAttempts%
{
    ImageSearch, n42x, n42y, ...
    if (errorlevel = 0)
        break
    if (errorlevel = 2)
    {
        msgbox, 이미지 파일을 찾을 수 없음
        return
    }
    sleep 100
}
if (A_Index >= maxAttempts)
{
    msgbox, 이미지를 찾을 수 없습니다 (타임아웃)
    return
}
```

---

## 3. 최적화 포인트

### 3.1 Excel Range 반복 접근 (심각도: 중간)

**문제 위치:** 3892-3908줄 등

```autohotkey
Loop, % 행1
{
    행++
    날짜 := XL.ActiveSheet.Range("A" 행).value
    시간 := XL.ActiveSheet.Range("AK" 행).value
    ; ... 매 루프마다 개별 셀 접근
}
```

**문제점:** 매번 COM 호출로 인한 성능 저하

**해결 방법:** 범위를 한 번에 읽기
```autohotkey
; 전체 범위를 한 번에 배열로 읽기
dataRange := XL.ActiveSheet.Range("A1:AX" 행1).Value
Loop, % 행1
{
    날짜 := dataRange[A_Index, 1]
    시간 := dataRange[A_Index, 37]  ; AK = 37번째 열
    ; ...
}
```

---

### 3.2 불필요한 Sleep 과다 사용 (심각도: 낮음)

**문제 위치:** 전체 스크립트

```autohotkey
Sleep, 500
Sleep, 500  ; 연속된 Sleep
; 또는
Sleep, 100
Sleep, 100
Sleep, 100  ; 분할된 Sleep
```

**해결 방법:** Sleep 통합 및 최소화
```autohotkey
Sleep, 1000  ; 필요한 만큼만 한 번에
```

---

### 3.3 중복 코드 (심각도: 중간)

**문제점:** 문자발송1, 문자발송2, 완료발송1, 완료발송2 등 유사한 코드 반복

**해결 방법:** 함수화
```autohotkey
SendSMS(phoneNumber, message, windowTitle) {
    ; 공통 로직
}

문자발송1:
{
    SendSMS(전화번호1, 조합1내용, WINTITLE)
}
return
```

---

### 3.4 Gui Destroy/Create 반복 (심각도: 낮음)

**문제 위치:** 855-867줄 등

```autohotkey
재단컷:
{
    Gui, 3:Destroy
    Gui, 3:Default
    ; ... GUI 재생성
    Gui, 3:Show
}
```

**해결 방법:** GUI를 한 번 생성하고 Show/Hide 사용
```autohotkey
; 초기화 시 한 번만 생성
Gui, 3:Add, ...

; 필요 시
Gui, 3:Show
; 또는
Gui, 3:Hide
```

---

## 4. 기타 개선사항

### 4.1 에러 처리 강화

```autohotkey
; 현재
result := dbQuery(myDB, myQuery)
if(errorCheck(result)){
    MsgBox, % "Error"
}

; 개선
result := dbQuery(myDB, myQuery)
if(errorCheck(result)){
    MsgBox, % "Error: " result[2] " - " result[3]
    result := ""  ; 메모리 해제
    return  ; 또는 적절한 복구 로직
}
```

### 4.2 전역 변수 정리

**문제점:** 많은 전역 변수 사용으로 메모리 상주

**권장사항:**
- 필요한 전역 변수만 명시적으로 선언
- 함수 내 지역 변수 활용

---

## 5. 즉시 수정 권장 사항

| 우선순위 | 항목 | 영향도 |
|----------|------|--------|
| 1 | OnExit에 MySQL 연결 해제 추가 | 높음 |
| 2 | Excel COM 객체 (wb, ws) 해제 추가 | 높음 |
| 3 | ImageSearch 루프에 타임아웃 추가 | 높음 |
| 4 | Excel Range 일괄 읽기로 변경 | 중간 |
| 5 | 불필요한 Sleep 제거/통합 | 낮음 |

---

## 6. 수정 코드 예시

### OnExit 핸들러 추가 (스크립트 시작 부분에)

```autohotkey
; 113줄 이후에 추가
OnExit("CleanUpOnExit")

CleanUpOnExit() {
    global myDB
    try {
        dbDisConnect(myDB)
        exit_sql()
    }
    return 0  ; 종료 허용
}
```

---

리뷰 일자: 2026-01-05
